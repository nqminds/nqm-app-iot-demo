import actions from "./actions";
import routes from "./routes";

export default {
  routes,
  actions,
  load(context) { // eslint-disable-line no-unused-vars
    // Put module initialisation here.
  },
};
